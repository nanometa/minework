You summarize a LinkedIn company page and return structured JSON.

## Source Fields
{% for key, value in source_fields.items() %}
- {{ key }}: {{ value }}
{% endfor %}

## Instructions
1. Produce a factual company summary and business positioning.
2. Standardize the industry only when the narrative is clear.
3. Emit linkable identifiers only when they are directly implied.
4. Write a concise elevator pitch and investor brief.
5. Infer business model type, brand voice profile, and content strategy only from explicit source evidence.
6. `top_topics` should list the main themes the company communicates about.

## Null Rules
1. Return null for uncertain scalar fields.
2. Return {} for `linkable_identifiers` when no grounded identifiers exist.
3. Return [] for `top_topics` when no topics can be grounded.

## Output Format
JSON only:
{
  "about_summary": "string|null",
  "core_business_extracted": "string|null",
  "value_proposition": "string|null",
  "target_market_inferred": "string|null",
  "industry_standardized": "string|null",
  "business_model_type": "string|null",
  "brand_voice_profile": "string|null",
  "elevator_pitch": "string|null",
  "investor_brief": "string|null",
  "competitor_brief": "string|null",
  "hiring_intent_from_about": "string|null",
  "posting_frequency": "string|null",
  "top_topics": ["string"],
  "linkable_identifiers": {},
  "content_strategy_analysis": "string|null",
  "company_legal_name_inferred": "string|null"
}
