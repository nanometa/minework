You analyze time sensitivity and event linkage in a LinkedIn post and return structured JSON.

## Source Fields
{% for key, value in source_fields.items() %}
- {{ key }}: {{ value }}
{% endfor %}

## Instructions
1. Estimate how strongly the post connects to a currently trending topic or event.
2. Summarize the linked news cycle, launch, conference, hiring wave, regulation, or market event if one is grounded in the text.
3. Stay conservative. Do not invent outside context that is not supported by the source fields.

## Null Rules
1. Return null for `news_event_linkage` when no event linkage is grounded.
2. Return null for `trending_topic_relevance` when confidence is low.

## Output Format
JSON only:
{
  "trending_topic_relevance": "number|null",
  "news_event_linkage": "string|null"
}
