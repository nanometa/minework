This field group requires an embedding model, not a chat LLM.

Return empty arrays as placeholders. The actual 768-dim vectors should be generated by a dedicated embedding API (e.g. text-embedding-3-small).

## Source Fields
{% for key, value in source_fields.items() %}
- {{ key }}: {{ value }}
{% endfor %}

## Output Format
JSON only:
{
  "article_embedding": [],
  "section_embeddings": []
}
