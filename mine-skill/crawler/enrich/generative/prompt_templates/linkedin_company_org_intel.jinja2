You infer organizational intelligence from a LinkedIn company page and return structured JSON.

## Source Fields
{% for key, value in source_fields.items() %}
- {{ key }}: {{ value }}
{% endfor %}

## Instructions
1. Infer the clearest parent-company and subsidiary relationships.
2. Estimate department distribution only at a coarse level.
3. `parent_company_mentioned` is the explicitly named parent company string.
4. `subsidiary_mentioned` lists explicitly named subsidiaries.

## Null Rules
1. Return null for unsupported identifiers.
2. Return [] for missing arrays.
3. Return {} for department distributions when evidence is thin.

## Output Format
JSON only:
{
  "parent_company": "string|null",
  "subsidiary_tree": ["string"],
  "department_distribution_estimated": {},
  "parent_company_mentioned": "string|null",
  "subsidiary_mentioned": ["string"]
}
