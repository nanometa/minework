# ── Build / cache ──
__pycache__/
*.egg-info/
dist/
.coverage
htmlcov/
.pytest-tmp/
.pytest_cache/
pytest-cache-files-*/
.tmp-openclaw-test/
.mypy_cache/
.ruff_cache/

# ── Local virtualenvs ──
.venv/
venv/
.mine-setup.json

# ── Secrets (see .env.example) ──
.env
.env.*
!.env.example

# ── Runtime output (root level only, not crawler/output/) ──
/output/
.sessions/
.omx/
.tmp-debug*/

# ── Dev artifacts (drafts, temp output) ──
.dev/
test_data/
test_output/
local_tasks/

# ── Docs not needed in production ──
docs/TEST_REPORT_*.md
docs/superpowers/

mine-workspace/
example-worker.py